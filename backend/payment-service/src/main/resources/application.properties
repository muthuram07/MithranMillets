spring.application.name=payment-service
server.port=${PORT:8084}

spring.datasource.url=jdbc:mysql://${DB_HOST:mithranmillets-muthuraman31-db0d.e.aivencloud.com}:${DB_PORT:24610}/paymentdb?sslMode=REQUIRED
spring.datasource.username=${DB_USERNAME:avnadmin}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver



spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true


razorpay.key=${RAZORPAY_KEY}
razorpay.secret=${RAZORPAY_SECRET}
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5174,https://mithran-frontend.onrender.com}
eureka.client.service-url.defaultZone=${EUREKA_URL:http://localhost:8761/eureka}
eureka.client.enabled=${EUREKA_ENABLED:true}
jwt.secret=${JWT_SECRET}



